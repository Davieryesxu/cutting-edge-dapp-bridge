[project]
name = "cutting-edge-dapp-bridge"
authors = ["CuttingEdge Developer Team"]
description = "Cross-platform transaction escrow and bridging infrastructure"
telemetry = false
requirements = []
[contracts.bridge-transaction-escrow]
path = "contracts/bridge-transaction-escrow.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
